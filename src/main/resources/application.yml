spring:
  application:
    name: cache-demo
  datasource:
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
  h2:
    console:
      enabled: true
  redis:
    host: localhost
    port: 6379
    url: redis://localhost:6379
    timeout: 500ms
server:
  port: 9101